# TODO

- [ ] Add dynamic zone state inversion configuration at runtime (allow updating zone inversion settings after connection / on an active `SatelIntegra` instance).
- [ ] Add output state inversion support (`outputs_invert` in `Config` / parser to logically invert reported output states `ON <-> OFF`).
- [ ] Add `partitions_count: u8` field to `IntegraVersion` alongside `io_count` to directly expose the maximum number of supported partitions for the panel model (4 for Integra 24, 8 for 32/64, 16 for 128, 32 for 256).
- [ ] Fix cached temperature reading: check if temperature was actually read before returning. If a sensor failed or has `TemperatureSensorStatus::NoRead` / error status, return `None` (or `Err(SatelError::TemperatureNoRead)`) instead of returning misleading default `0.0°C`.
- [ ] Smart aggregated trouble processing: provide high-level multi-part aggregation (`get_active_troubles()`, `get_cached_active_troubles()`) to query all 8 trouble parts and return a consolidated list of active issues with descriptive names and categories.
- [ ] Smart output state verification: provide optional/smart post-command verification after `set_output_on`/`set_output_off`/`set_output_toggle` to check if the output actually changed state, detecting silent panel drops caused by wrong PIN, lack of permissions, or non-switchable outputs.
- [ ] Smart partition state verification: provide optional/smart post-command verification after `arm_full`/`arm_stay`/`disarm`/`clear_alarm` to check if the partition actually transitioned to the desired state, detecting silent panel drops caused by wrong PIN, unauthorized access, or unbypassed zone violations.
- [x] Add temperature error event: introduce `SatelEvent::ZoneTemperatureError { id: u16, status: TemperatureSensorStatus }` to broadcast sensor faults, timeouts, and broken probe states to channel subscribers asynchronously.
- [x] Background temperature auto-poller worker: implement an automated background polling task in `Config` (`polling_temperatures: bool`, `polling_temperatures_zones: Vec<u16>`, `polling_temperatures_interval_minutes: u64`) to periodically query configured wireless/wired temperature sensors in the background, updating cache and emitting `ZoneTemperatureChanged` events without requiring manual polling loops in user applications.
